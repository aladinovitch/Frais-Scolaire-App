@model IEnumerable<Frais_Scolaire.Models.Versement>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Etudiant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paiement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Etudiant.Fullname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Paiement.Nom)
            </td>
        </tr>
}
    </tbody>
</table>
